# Docker Environment Variables
SECRET_KEY=django-docker-secret-key-change-in-production
DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.development

# Allowed Hosts for Docker
ALLOWED_HOSTS=localhost,127.0.0.1,web,0.0.0.0

# Database Configuration (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=django_starter
DB_USER=django_user
DB_PASSWORD=django_password
DB_HOST=db
DB_PORT=5432

# Redis Configuration
REDIS_URL=redis://redis:6379/1

# Email Configuration (Console backend for development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Time Zone
TIME_ZONE=America/New_York


# Django Social Auth - OAuth Provider Configuration (fill in your own values)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# Cloudflare Turnstile Configuration (fill in your own values)
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=